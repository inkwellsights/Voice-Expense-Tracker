.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/
data/
*.log
.DS_Store
.idea/
.vscode/

# Host-specific Compose overrides (e.g. port remaps for an existing
# Cloudflare Tunnel ingress). Each box keeps its own; never committed.
docker-compose.override.yml
docker-compose.override.yaml
